// Creation Date: #DATETIME#
// Author(s): #USERNAME#

using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Assertions;

#ROOTNAMESPACEBEGIN#
public class #SCRIPTNAME# : MonoBehaviour
{
	void Awake()
	{
		#NOTRIM#
	}

	void Start()
	{
		#NOTRIM#
	}

	void Update()
	{
		// Please use Time.deltaTime for anything that should be paused while the start menu is open (or check if Time.timeScale > 0)
		// Use Time.unscaledDeltaTime for things that should be framerate independant but should continue happening even when the game is paused
		#NOTRIM#
	}

	void FixedUpdate()
	{
		#NOTRIM#
	}
}
#ROOTNAMESPACEEND#
